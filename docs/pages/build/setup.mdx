
// Protótipo interativo: Conexão de Energia Inteligente-SAP// Arquivo React de exemplo (Tailwind CSS) // Este componente funciona como um protótipo UI de alta fidelidade para apresentação. // Inclui telas: Dashboard, Carteira (Wallet), Localizar Postos, Gestão de Frotas, Marketplace, Relatórios e Suporte (Chat). // Observação: Dados mock; este arquivo foi criado para visualização e interação rápida.

import React, { useState } from "react";

export default function PrototipoConexãodeEnergiaInteligente-SAP() { const tabs = [ "Dashboard", "Carteira", "Localizar", "Frotas", "Marketplace", "Relatórios", "Suporte", ];

const [active, setActive] = useState("Dashboard");

// Dados de exemplo const stations = [ { id: 1, name: "Posto Central - Luanda", fuel: "Gasolina", price: 300,00, green: true, dist: "1.2 km" }, { id: 2, name: "Posto Norte - Cacuaco", fuel: "Diesel", price: 400,00, green: false, dist: "8.4 km" }, { id: 3, name: "Posto Sul - Talatona", fuel: "Gás Natural", price: 1.200,00, green: true, dist: "12 km" }, ];

const fleet = [ { id: "F001", name: "Camião 1", odometer: 120000, fuelAvg: 9.4, status: "OK" }, { id: "F002", name: "Pick-up 3", odometer: 45000, fuelAvg: 7.8, status: "Manutenção" }, ];

const wallet = { balance: 12500.50, currency: "AOA", points: 1080 };

return ( <div className="min-h-screen flex bg-gray-50 text-gray-900"> {/* Sidebar */} <aside className="w-72 bg-white border-r shadow-sm p-6"> <h1 className="text-2xl font-bold text-slate-800">Smart Energy Connect</h1> <p className="text-sm text-gray-500 mt-1">Protótipo para Sonangol</p>

<nav className="mt-6 space-y-2">
      {tabs.map((t) => (
        <button
          key={t}
          onClick={() => setActive(t)}
          className={`w-full text-left px-3 py-2 rounded-md transition-all ${
            active === t
              ? "bg-sky-500 text-white shadow"
              : "text-gray-700 hover:bg-gray-100"
          }`}
        >
          {t}
        </button>
      ))}
    </nav>

    <div className="mt-6 border-t pt-4 text-sm text-gray-600">
      <div className="mb-2">Usuário: <strong>Empresa X</strong></div>
      <div>Status: <span className="text-green-600 font-medium">Ativo</span></div>
      <div className="mt-3">
        <button className="px-3 py-2 bg-slate-100 rounded text-sm">Configurações</button>
      </div>
    </div>
  </aside>

  {/* Main area */}
  <main className="flex-1 p-8">
    <header className="flex items-center justify-between mb-6">
      <div>
        <h2 className="text-2xl font-semibold">{active}</h2>
        <p className="text-sm text-gray-500 mt-1">Protótipo navegável — dados mock</p>
      </div>
      <div className="flex items-center gap-4">
        <div className="text-sm text-right">
          <div className="text-gray-600">Saldo (carteira)</div>
          <div className="font-semibold text-lg">{wallet.currency} {wallet.balance.toLocaleString()}</div>
        </div>
        <div className="w-10 h-10 bg-sky-500 rounded-full flex items-center justify-center text-white font-bold">SE</div>
      </div>
    </header>

    <section>
      {active === "Dashboard" && (
        <Dashboard stations={stations} fleet={fleet} wallet={wallet} />
      )}

      {active === "Carteira" && <Carteira wallet={wallet} />}

      {active === "Localizar" && <Localizar stations={stations} />}

      {active === "Frotas" && <Frotas fleet={fleet} />}

      {active === "Marketplace" && <Marketplace />}

      {active === "Relatórios" && <Relatorios />}

      {active === "Suporte" && <Suporte />}
    </section>
  </main>
</div>

); }

/* ---------- Componentes de cada tela (simplificados para protótipo) ---------- */

function Dashboard({ stations, fleet, wallet }) { return ( <div className="space-y-6"> <div className="grid grid-cols-3 gap-6"> <Card title="Postos próximos"> {stations.map((s) => ( <div key={s.id} className="flex items-center justify-between py-2"> <div> <div className="font-medium">{s.name}</div> <div className="text-xs text-gray-500">{s.fuel} • {s.dist}</div> </div> <div className="text-sm text-gray-700">{s.price} $</div> </div> ))} </Card>

<Card title="Status da Frota">
      <ul className="space-y-2">
        {fleet.map((v) => (
          <li key={v.id} className="flex justify-between">
            <div>
              <div className="font-medium">{v.name}</div>
              <div className="text-xs text-gray-500">OD: {v.odometer} km</div>
            </div>
            <div className={`text-sm font-semibold ${v.status === 'OK' ? 'text-green-600' : 'text-amber-600'}`}>{v.status}</div>
          </li>
        ))}
      </ul>
    </Card>

    <Card title="Engajamento & Sustentabilidade">
      <div className="text-sm text-gray-600">Pontos acumulados: <strong>1.080</strong></div>
      <div className="mt-3 text-sm text-gray-600">CO₂ evitado (estimado): <strong>12 tCO₂</strong></div>
      <div className="mt-4">
        <button className="px-3 py-2 bg-sky-500 text-white rounded">Ver oportunidades verdes</button>
      </div>
    </Card>
  </div>

  <div className="grid grid-cols-2 gap-6">
    <Card title="Mapa Operacional">
      <div className="h-72 bg-gradient-to-br from-white to-gray-100 rounded border flex items-center justify-center text-gray-400">
        [Mapa interativo - placeholder]
      </div>
    </Card>

    <Card title="Resumo Financeiro">
      <div className="space-y-2">
        <div>Receita digital mensal: <strong>+15%</strong></div>
        <div>Economia média por frota: <strong>18%</strong></div>
        <div>Usuários ativos (Mês): <strong>42k</strong></div>
      </div>
    </Card>
  </div>
</div>

); }

function Carteira({ wallet }) { const tx = [ { id: 1, label: "Abastecimento Posto Central", value: -4500, date: '2025-09-01' }, { id: 2, label: "Recarga PTS", value: -1200, date: '2025-08-28' }, { id: 3, label: "Crédito recebido", value: 5000, date: '2025-08-15' }, ];

return ( <div className="grid grid-cols-3 gap-6"> <Card title="Saldo"> <div className="text-3xl font-bold">AOA {wallet.balance.toLocaleString()}</div> <div className="text-sm text-gray-500 mt-2">Pontos: {wallet.points}</div> <div className="mt-4"> <button className="px-3 py-2 bg-green-600 text-white rounded">Adicionar fundos</button> </div> </Card>

<Card title="Transações recentes" className="col-span-2">
    <ul className="space-y-3">
      {tx.map((t) => (
        <li key={t.id} className="flex justify-between">
          <div>
            <div className="font-medium">{t.label}</div>
            <div className="text-xs text-gray-500">{t.date}</div>
          </div>
          <div className={`font-semibold ${t.value < 0 ? 'text-red-600' : 'text-green-600'}`}>{t.value < 0 ? '-' : '+'} AOA {Math.abs(t.value).toLocaleString()}</div>
        </li>
      ))}
    </ul>
  </Card>
</div>

); }

function Localizar({ stations }) { return ( <div className="grid grid-cols-2 gap-6"> <Card title="Mapa de Postos"> <div className="h-96 bg-gray-100 rounded flex items-center justify-center text-gray-400">[Mapa interativo - placeholder]</div> </Card>

<Card title="Lista de Postos">
    <div className="space-y-3">
      {stations.map((s) => (
        <div key={s.id} className="p-3 border rounded flex justify-between items-center">
          <div>
            <div className="font-medium">{s.name}</div>
            <div className="text-xs text-gray-500">{s.fuel} • {s.green ? 'Posto Verde' : 'Convencional'}</div>
          </div>
          <div className="text-right">
            <div className="font-semibold">{s.price} $</div>
            <div className="text-xs text-gray-500">{s.dist}</div>
            <div className="mt-2">
              <button className="px-2 py-1 bg-sky-500 text-white rounded text-xs">Navegar</button>
            </div>
          </div>
        </div>
      ))}
    </div>
  </Card>
</div>

); }

function Frotas({ fleet }) { return ( <div className="space-y-6"> <Card title="Visão Geral da Frota"> <table className="w-full text-sm"> <thead className="text-left text-gray-600"> <tr><th>Veículo</th><th>OD</th><th>Consumo (L/100km)</th><th>Status</th></tr> </thead> <tbody> {fleet.map((v) => ( <tr key={v.id} className="border-t"> <td className="py-2">{v.name}</td> <td>{v.odometer} km</td> <td>{v.fuelAvg}</td> <td className={${v.status==='OK' ? 'text-green-600' : 'text-amber-600'}}>{v.status}</td> </tr> ))} </tbody> </table> </Card>

<Card title="Alertas de Manutenção">
    <ul className="space-y-3 text-sm text-gray-700">
      <li>Pick-up 3: Trocar óleo (vencido)</li>
      <li>Camião 1: Pneus - verificação</li>
    </ul>
  </Card>
</div>

); }

function Marketplace() { const offers = [ { id: 1, title: 'Óleo lubrificante - 5L', price: '2.500 AOA' }, { id: 2, title: 'Check-up rápido', price: '5.000 AOA' }, { id: 3, title: 'Carregamento EV - 50kWh', price: '10.000 AOA' }, ];

return ( <div className="grid grid-cols-3 gap-6"> {offers.map(o => ( <div key={o.id} className="p-4 border rounded bg-white"> <div className="font-medium">{o.title}</div> <div className="text-sm text-gray-500">{o.price}</div> <div className="mt-3"> <button className="px-3 py-2 bg-sky-500 text-white rounded">Comprar</button> </div> </div> ))} </div> ); }

function Relatorios() { return ( <div className="grid grid-cols-2 gap-6"> <Card title="Relatório Emissões"> <div className="text-sm text-gray-700">CO₂ evitado: <strong>12 tCO₂</strong></div> <div className="mt-3 text-sm text-gray-500">Comparado ao mês anterior: <strong>-4%</strong></div> </Card>

<Card title="Relatório Financeiro">
    <div className="text-sm">Receita digital: <strong>+15%</strong></div>
    <div className="mt-2 text-sm text-gray-500">Transações (mês): <strong>8.200</strong></div>
  </Card>
</div>

); }

function Suporte() { const [messages, setMessages] = useState([ { from: 'bot', text: 'Olá! Como posso ajudar?' }, ]); const [txt, setTxt] = useState('');

function send() { if (!txt) return; setMessages(m => [...m, { from: 'user', text: txt }]); setTxt(''); setTimeout(() => { setMessages(m => [...m, { from: 'bot', text: 'Obrigado — vamos verificar e responder em breve.' }]); }, 800); }

return ( <div className="grid grid-cols-3 gap-6"> <div className="col-span-2"> <Card title="Chat de Suporte"> <div className="h-72 overflow-auto p-3 bg-gray-50 rounded"> {messages.map((m, i) => ( <div key={i} className={mb-3 ${m.from === 'user' ? 'text-right' : 'text-left'}}> <div className={${m.from === 'user' ? 'inline-block bg-sky-500 text-white' : 'inline-block bg-white border'} rounded px-3 py-2}>{m.text}</div> </div> ))} </div> <div className="mt-3 flex gap-2"> <input value={txt} onChange={e => setTxt(e.target.value)} className="flex-1 px-3 py-2 border rounded" placeholder="Escreva sua mensagem..." /> <button onClick={send} className="px-4 py-2 bg-sky-500 text-white rounded">Enviar</button> </div> </Card> </div>

<Card title="Artigos rápidos" className="h-fit">
    <ul className="text-sm space-y-2">
      <li><strong>FAQ:</strong> Como usar a carteira?</li>
      <li><strong>Guia:</strong> Como instalar um carregador EV?</li>
      <li><strong>Contato:</strong> suporte@sonangol.co.ao</li>
    </ul>
  </Card>
</div>

); }

/* ---------- Pequeno componente Card para layout ---------- */ function Card({ title, children, className = '' }) { return ( <div className={bg-white p-4 rounded shadow-sm border ${className}}> {title && <div className="font-semibold mb-3">{title}</div>} <div>{children}</div> </div> ); }

/* Fim do arquivo de protótipo */

